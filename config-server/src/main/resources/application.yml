server:
  port: 8090

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sajeevan98/Application-Config/

# Eureka service registry.
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
